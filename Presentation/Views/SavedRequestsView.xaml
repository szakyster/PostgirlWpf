<UserControl x:Class="Postgirl.Presentation.Views.SavedRequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Border Padding="4,2">
		<StackPanel>

			<ListBox ItemsSource="{Binding SavedRequests}" MouseDoubleClick="OnDoubleClick" Background="{StaticResource Brush.Surface}">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<Border Padding="2,3">
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*"/>
									<ColumnDefinition Width="Auto"/>
								</Grid.ColumnDefinitions>

								<StackPanel Grid.Column="0">
									<TextBlock Text="{Binding Name}"
											   FontWeight="SemiBold"/>
									<TextBlock Text="{Binding Url}"
											   FontSize="11"
											   Foreground="Gray"
											   TextTrimming="CharacterEllipsis"/>
								</StackPanel>

								<Button Grid.Column="1"
										Content="✖"
										Command="{Binding DataContext.DeleteSavedRequestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
										CommandParameter="{Binding}"
										ToolTip="Delete saved request"
										Padding="4"
										Margin="4,0,0,0"
										VerticalAlignment="Center"
										Style="{StaticResource GhostButtonStyle}"/>
							</Grid>
						</Border>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>

		</StackPanel>
	</Border>

</UserControl>