<UserControl x:Class="Postgirl.Presentation.Views.RequestDocument.Parts.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Border 
	        Background="#F0F0F0"
	        BorderBrush="#D0D0D0"
	        BorderThickness="0,1,0,0"
	        Margin="0,8,0,0"
	        Padding="8,4"
	        Visibility="{Binding StatusCode, Converter={StaticResource StatusCodeToVisibilityConverter}}"
	>
		<StackPanel Orientation="Horizontal"
		            VerticalAlignment="Center">
			<TextBlock Text="{Binding StatusCode}"
			           Foreground="{Binding StatusColor}"
			           FontWeight="SemiBold"
			           FontFamily="Consolas"
			           MinWidth="36"/>
			<TextBlock Text="{Binding StatusText}"
			           Margin="8,0,0,0"
			           Foreground="#666"
			           VerticalAlignment="Center"/>
			<TextBlock Text="{Binding ElapsedMilliseconds, StringFormat='{}{0} ms'}"
			           Margin="8,0,0,0"
			           Foreground="#666"
			           FontFamily="Consolas"
			           VerticalAlignment="Center"/>
		</StackPanel>
	</Border>
</UserControl>