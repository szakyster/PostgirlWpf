<UserControl x:Class="Postgirl.Presentation.Views.RequestDocument.Parts.RequestAuthView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:auth="clr-namespace:Postgirl.Domain.Authentication"
			 xmlns:controls="clr-namespace:Postgirl.Presentation.Controls">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="220"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<!-- LEFT -->
		<StackPanel Grid.Column="0"
			            Margin="8">

			<TextBlock Text="Authentication Type"
				           FontWeight="SemiBold"
				           Margin="0,0,0,6"/>

			<ComboBox ItemsSource="{Binding Auth.AvailableAuthTypes}"
				          SelectedItem="{Binding Auth.AuthType}" />
		</StackPanel>

		<!-- RIGHT -->
		<Border Grid.Column="1"
Margin="8"
BorderBrush="#DDD"
BorderThickness="1"
CornerRadius="4"
Padding="12">

			<ContentControl>

				<ContentControl.Style>
					<Style TargetType="ContentControl">

						<!-- default: empty -->
						<Setter Property="Content" Value="{x:Null}" />

						<Style.Triggers>

							<!-- NO AUTH -->
							<DataTrigger Binding="{Binding Auth.AuthType}"
                         Value="{x:Static auth:AuthType.None}">
								<Setter Property="Content">
									<Setter.Value>
										<TextBlock Text="This request does not use any authorization."
                                   Foreground="Gray"
                                   FontStyle="Italic"
                                   TextWrapping="Wrap"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
									</Setter.Value>
								</Setter>
							</DataTrigger>

							<!-- BEARER TOKEN -->
							<DataTrigger Binding="{Binding Auth.AuthType}"
                         Value="{x:Static auth:AuthType.BearerToken}">
								<Setter Property="Content">
									<Setter.Value>

										<Grid>
											<Grid.RowDefinitions>
												<RowDefinition Height="Auto"/>
												<RowDefinition Height="*"/>
												<RowDefinition Height="Auto"/>
											</Grid.RowDefinitions>

											<TextBlock Grid.Row="0"
													   Text="Bearer Token"
													   FontWeight="SemiBold"
													   Margin="0,0,0,6"/>

											<controls:VariableAwareTextBox
													Grid.Row="1"
													Text="{Binding Auth.BearerToken, UpdateSourceTrigger=PropertyChanged}"
													AcceptsReturn="False"
													MinWidth="300" />

											<TextBlock Grid.Row="2"
													   Text="The token will be sent in the Authorization header."
													   Margin="0,6,0,0"
													   Foreground="Gray"
													   FontSize="11"/>

										</Grid>
									</Setter.Value>
								</Setter>
							</DataTrigger>

							<!-- WINDOWS AUTHENTICATION -->
							<DataTrigger Binding="{Binding Auth.AuthType}"
                         Value="{x:Static auth:AuthType.WindowsAuthentication}">
								<Setter Property="Content">
									<Setter.Value>
										<TextBlock Text="The request will use the current Windows user's credentials."
                                   Foreground="Gray"
                                   FontStyle="Italic"
                                   TextWrapping="Wrap"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
									</Setter.Value>
								</Setter>
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</ContentControl.Style>
			</ContentControl>
		</Border>
	</Grid>
</UserControl>